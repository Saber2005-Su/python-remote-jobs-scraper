# python-remote-jobs-scraper
Python web scraping project that extracts remote job listings from a public API and saves structured data (position, company, location, apply link) into a CSV file for analysis and portfolio use.
